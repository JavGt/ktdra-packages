import { Color, PaletteKeys } from '@ktdra-digital/utils';

export type ContainerShadowProps = {
  text: string;
  paletteKey?: PaletteKeys;
};

export type ContainerShadowStlProps = {
  colors: Color;
};
